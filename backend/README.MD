# Backend

See the [root README](../README.md) for setup, scripts, authentication, tests, and deployment. Express is created in `src/app.js`; `src/index.js` loads configuration, connects MongoDB, and starts listening.
